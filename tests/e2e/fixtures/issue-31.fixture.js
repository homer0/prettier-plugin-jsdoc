module.exports = {
  singleQuote: false,
};

//# input

/**
 * @typedef {Pick<Account, "email" | "password" | "role">} AccountCreationArgs
 */

//# output

/**
 * @typedef {Pick<Account, "email" | "password" | "role">} AccountCreationArgs
 */
